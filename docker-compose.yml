version: '3'
services:
  web:
    # 👇 对应你 GitHub Action 里的 tags
    image: 18879011721/next-ssr-app:latest
    container_name: next-ssr-server
    restart: always
    ports:
      # ✅ 左边是服务器端口，右边是容器端口 (都在 Dockerfile 里定死了 4007)
      - '4007:4007'
    environment:
      - TZ=Asia/Shanghai
