version: '3'
services:
  deeplink-monitor:
    image: 18879011721/next-ssr-app:latest
    container_name: next-ssr-server
    restart: unless-stopped
    ports:
      # 左边宿主机端口 : 右边容器端口 (Dockerfile里我们定死了4007)
      - '4007:4007'
    networks:
      # ✅ 加入你现有的网络，这样它就能和 postgres/mongo 容器互通了
      - degpt-network
    environment:
      - TZ=Asia/Shanghai
      - HOSTNAME=0.0.0.0
      - PORT=4007
      # 👇 数据库连接 (下面详细解释)
      - MONGODB_URI=mongodb://192.238.204.152:27017/deeplink-monitor

# 声明这是一个外部已经存在的网络
networks:
  degpt-network:
    external: true
